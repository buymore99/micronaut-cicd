name: cicd-demo
on:
  push:
    branches:
      - '*'

jobs:
  build-job:
    name: Build job
    runs-on: ubuntu-latest
    steps:
      - name: 'Start build job'
        run: |
          echo "Starting the build job."